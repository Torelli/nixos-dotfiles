function control-l --wraps=clear --description 'alias clear control-l'
  clear
end

function ctrl-l -wraps=clear --description 'alias clear ctrl-l'
  clear
end

function C-l -wraps=clear --description 'alias clear C-l'
  clear
end
